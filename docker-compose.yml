services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: chiirl
      POSTGRES_PASSWORD: chiirl
      POSTGRES_DB: chiirl
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
