JSON.stringify(Array.from(document.querySelectorAll('.event-card')).map(function(card) { var a = card.querySelector('a.event-card-link'); if (!a) return null; var href = a.getAttribute('href') || ''; if (!href.includes('/e/')) return null; var h3 = card.querySelector('h3'); var title = h3 ? h3.textContent.trim() : ''; var img = card.querySelector('img.event-card-image'); var imageUrl = img ? img.getAttribute('src') || '' : ''; var ps = card.querySelectorAll('section.event-card-details p'); var dateText = ''; var location = ''; for (var i = 0; i < ps.length; i++) { var t = ps[i].textContent.trim(); if (!dateText && /\b(Mon|Tue|Wed|Thu|Fri|Sat|Sun|Today|Tomorrow)\b/i.test(t)) { dateText = t; } else if (!location && dateText && t.length > 1 && !/^From\s*\$/.test(t)) { location = t; } } return { title: title, href: href.split('?')[0], date_text: dateText, location: location || (a.getAttribute('data-event-location') || ''), image_url: imageUrl }; }).filter(function(e) { return e && e.title; }).filter(function(e, i, arr) { return i === arr.findIndex(function(o) { return o.href === e.href; }); }))