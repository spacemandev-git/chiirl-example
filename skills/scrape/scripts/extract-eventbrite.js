JSON.stringify(Array.from(document.querySelectorAll('a[href*="eventbrite.com/e/"]')).map(function(a) { var href = a.getAttribute('href') || ''; var h3 = a.querySelector('h3'); var title = h3 ? h3.textContent.trim() : ''; var img = a.querySelector('img[src]'); var imageUrl = img ? img.getAttribute('src') || '' : ''; var ps = a.querySelectorAll('p'); var dateText = ''; var location = ''; if (ps.length >= 1) dateText = ps[0].textContent.trim(); if (ps.length >= 2) location = ps[1].textContent.trim(); return { title: title, href: href.split('?')[0], date_text: dateText, location: location, image_url: imageUrl }; }).filter(function(e) { return e.title; }))