JSON.stringify(Array.from(document.querySelectorAll('h3')).map(function(h) { var p = h; for (var i = 0; i < 8; i++) { p = p.parentElement; if (!p) break; var a = p.querySelector('a[href]'); if (a) { var href = a.getAttribute('href') || ''; if (href.match(/^\/[a-z0-9]{6,}$/)) { var text = p.innerText || ''; var lines = text.split(String.fromCharCode(10)).map(function(l) { return l.trim(); }).filter(function(l) { return l.length > 0 && l !== String.fromCharCode(8203); }); var timeLine = lines.find(function(l) { return /\d{1,2}:\d{2}\s*(AM|PM)/.test(l); }) || ''; var byIdx = lines.findIndex(function(l) { return l.startsWith('By '); }); var location = byIdx >= 0 ? lines.slice(byIdx + 1).find(function(l) { return !l.startsWith('By ') && l.length > 1; }) || '' : ''; return { title: h.textContent.trim(), slug: href.slice(1), time: timeLine, location: location, description: '' }; } } } return null; }).filter(Boolean))