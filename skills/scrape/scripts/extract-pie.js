JSON.stringify((function() { var cards = document.querySelectorAll('[class*="PlanList"] a[href]'); if (cards.length === 0) cards = document.querySelectorAll('a[href*="/plans/"], a[href*="/plan/"], a[href*="/p/"]'); if (cards.length === 0) { var allLinks = document.querySelectorAll('a[href]'); cards = Array.from(allLinks).filter(function(a) { var h = a.getAttribute('href') || ''; return h.match(/\/p(lans?)?\/[a-zA-Z0-9_-]+/) && a.querySelector('img'); }); } return Array.from(cards).map(function(a) { var href = a.getAttribute('href') || ''; var imgs = a.querySelectorAll('img[src]'); var imageUrl = ''; for (var i = 0; i < imgs.length; i++) { var s = imgs[i].getAttribute('src') || ''; if (s && !s.includes('avatar') && !s.includes('profile')) { imageUrl = s; break; } } var allText = a.innerText || ''; var lines = allText.split(String.fromCharCode(10)).map(function(l) { return l.trim(); }).filter(function(l) { return l.length > 0; }); var title = ''; var dateLine = ''; var timeLine = ''; var venue = ''; for (var j = 0; j < lines.length; j++) { var l = lines[j]; if (!title && l.length > 3 && l.length < 200 && !/^\d+$/.test(l) && !/^(RSVP|Join|Free|Sold)/i.test(l)) { title = l; } else if (!dateLine && /\b(jan|feb|mar|apr|may|jun|jul|aug|sep|oct|nov|dec|mon|tue|wed|thu|fri|sat|sun|\d{1,2}\/\d{1,2})/i.test(l)) { dateLine = l; } else if (!timeLine && /\d{1,2}:\d{2}\s*(am|pm)/i.test(l)) { timeLine = l; } else if (!venue && title && l !== title && l.length > 3 && !/^\d+$/.test(l) && !/^(RSVP|Join|Free|Sold|Going|\d+ going)/i.test(l)) { venue = l; } } return { title: title, href: href, date_text: dateLine || timeLine, location: venue, image_url: imageUrl }; }).filter(function(e) { return e.title && e.href; }); })())