JSON.stringify(Array.from(document.querySelectorAll('a.PlanCard')).map(function(a) { var href = a.getAttribute('href') || ''; var h2 = a.querySelector('h2'); var title = h2 ? h2.textContent.trim() : ''; var img = a.querySelector('.PlanCard__image-and-title img'); var imageUrl = img ? img.getAttribute('src') || '' : ''; var dateEl = a.querySelector('.PlanCard__date-time'); var dateText = dateEl ? dateEl.textContent.trim() : ''; var locEl = a.querySelector('.PlanCard__location'); var location = locEl ? locEl.textContent.trim() : ''; return { title: title, href: href, date_text: dateText, location: location, image_url: imageUrl }; }).filter(function(e) { return e.title && e.href; }))